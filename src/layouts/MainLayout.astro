---
import '../styles/global.css';
import App from '../components/App.tsx';

interface Props {
  title: string;
  description?: string;
  image?: string;
  page?: string;
  lang?: string;
}

const { title, description = "Dr. Túlio Kalife - Especialista em Otorrinolaringologia. Consultas, diagnósticos e cirurgias otorrinolaringológicas.", image = "/images/og-image.png", page = 'home', lang = 'pt' } = Astro.props;
const whatsappNumber = import.meta.env.PUBLIC_WHATSAPP_NUMBER;
const canonicalURL = new URL(Astro.url.pathname, Astro.site); // Use full current pathname for canonical
const currentPath = Astro.url.pathname; // Keep locale in path for hreflang
---

<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/icons/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />

    <!-- Preconnect for Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- SEO Meta Tags -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="keywords" content={lang === 'en'
      ? 'otorhinolaryngology, doctor, consultations, surgeries, ear, nose, throat, Dr. Túlio Kalife'
      : lang === 'es'
      ? 'otorrinolaringología, médico, consultas, cirugías, oído, nariz, garganta, Dr. Túlio Kalife'
      : 'otorrinolaringologia, médico, consultas, cirurgias, ouvido, nariz, garganta, Dr. Túlio Kalife'
    } />
    <link rel="canonical" href={canonicalURL} />

    <!-- Hreflang for Multilingual SEO -->
    <link rel="alternate" hreflang="pt" href={`${Astro.site}${currentPath.replace(/^\/(en|es)/, '')}`} />
    <link rel="alternate" hreflang="en" href={`${Astro.site}${currentPath.replace(/^\/(pt|es)/, '/en')}`} />
    <link rel="alternate" hreflang="es" href={`${Astro.site}${currentPath.replace(/^\/(pt|en)/, '/es')}`} />
    <link rel="alternate" hreflang="x-default" href={`${Astro.site}${currentPath.replace(/^\/(en|es)/, '')}`} />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:type" content="website" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={image} />

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "MedicalBusiness",
      "name": "Dr. Túlio Kalife - Otorrinolaringologia",
      "description": description,
      "url": Astro.site,
      "logo": "/images/logo/logo.png",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "R. Cap. José Vicente de Moraes, 151 - Esplanada Mendes",
        "addressLocality": "São Roque",
        "addressRegion": "SP",
        "postalCode": "18130-780",
        "addressCountry": "BR"
      },
      "telephone": whatsappNumber,
      "sameAs": ["https://www.tuliootorrino.com"]
    })} />

    <!-- Apple Touch Icon -->
    <link rel="apple-touch-icon" href="/icons/apple-touch.png" />
  </head>
  <body>
    <App client:load page={page} whatsappNumber={whatsappNumber} />
  </body>
</html>